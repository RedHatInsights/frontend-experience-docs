name: Update services docs

on:
  schedule:
    - cron: '0 1 * * *'  # Run every day at 1 AM
  workflow_dispatch:  # Allow manual triggering

jobs:
  update-readmes:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4
        with:
          ref: master  # Ensure it checks out the master branch

      - name: Set up Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Make the update script executable
        run: chmod +x update_services.sh

      - name: Run the update_services.sh script
        run: ./update_services.sh

      - name: Verify README files in target directory
        run: |
          echo "Contents of pages/services after running the script:"
          ls -l pages/services

      - name: Check for untracked or modified files
        run: |
          echo "Running git status to check for untracked files:"
          git status  # Display status to check if files appear as untracked
          
          # List all untracked files for additional diagnostics
          echo "Listing untracked files only:"
          git ls-files --others --exclude-standard

      - name: Stage, commit, and push changes if present
        run: |
          # Add all markdown files in the services directory
          git add pages/services/*.md
          
          # Run git status again to confirm if files are now staged
          echo "Checking git status after adding files:"
          git status
          
          # Commit and push only if there are changes
          if [ -n "$(git status --porcelain)" ]; then
            BRANCH_NAME="update-services-docs-$(date +'%Y%m%d%H%M%S')"
            git commit -m "Update services docs with latest READMEs"
            git checkout -b "$BRANCH_NAME"
            git push origin "$BRANCH_NAME"
            echo "branch=$BRANCH_NAME" >> $GITHUB_ENV
          else
            echo "No changes to commit."
            echo "branch=" >> $GITHUB_ENV
          fi

      - name: Create Pull Request
        if: env.branch != ''
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: master
          branch: ${{ env.branch }}
          title: "Update Services in docs"
          body: "Update of the Services docs section with the latest README changes."
          labels: |
            documentation
